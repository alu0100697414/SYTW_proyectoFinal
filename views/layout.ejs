<!DOCTYPE html>
<html>

  <head>
    <meta charset="UTF-8">

    <title>QuizULL</title>

    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' type='text/css' media='(min-width: 950px)' href='/stylesheets/wide.css' />
    <link rel='stylesheet' type='text/css' media='(max-width: 480px)' href='/stylesheets/smartphone.css' />
  </head>


  <body bgcolor="#F5DEB3">
    <div id="page-wrap">

      <header class="main" id="h1">
        <h1>QuizULL<span>: El juego de las preguntas</span></h1>
      </header>


      <nav class="main" id="n1" role="navigation">
        <% if(!session.user){ %>
          <span class="right"><a href="/login" style="display:inline;">Iniciar sesión</a></span>
        <% } else { %>
          <span class="right"> <%=session.user.username%>
          <br>
          <a href="/logout" style="display:inline;">Cerrar sesión</a></span>
        <% } %>
        <br><br>
        <span><a href="/">Inicio</a></span>
        <br>
        <span><a href="/game">Jugar</a></span>
        <br>
        <span><a href="/quizes">Preguntas</a></span>
        <br>
        <span><a href="/instructions">Ayuda</a></span>
      </nav>


      <section class="main" id="s1">
        <% if(errors["pregunta"]) { %>
          <span class='ErrorMsgs'><%= errors["pregunta"] %></span>
        <% } %>
        <br>
        <% if(errors["respuesta"]) { %>
          <span class='ErrorMsgs'><%= errors["respuesta"] %></span>
        <% } %>

        <%- body %>
      </section>

    </div>
  </body>
</html>
